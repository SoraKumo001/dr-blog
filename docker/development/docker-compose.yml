services:
  postgres:
    container_name: dr-blog-postgres
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - dr-blog-vol:/var/lib/postgresql
    ports:
      - "25432:5432"
volumes:
  dr-blog-vol:
